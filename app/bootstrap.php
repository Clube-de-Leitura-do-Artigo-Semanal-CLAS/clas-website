<?php

/**
 * app/bootstrap.php
 *
 * Carrega tudo o que o resto da aplicação precisa antes de qualquer
 * pedido ser processado: variáveis de ambiente, autoload de classes,
 * ligação à base de dados.
 *
 * TODO:
 *   - carregar .env (ex: com uma função simples ou uma lib tipo vlucas/phpdotenv)
 *   - registar um autoloader (PSR-4 via composer, ou um autoloader manual
 *     se decidirmos não usar composer)
 *   - abrir a ligação PDO à base de dados (usar config/database.php)
 */

// Placeholder — substituir pela implementação real.
